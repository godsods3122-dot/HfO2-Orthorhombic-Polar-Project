# 노드 위치 재검증 — m1 (−1% compressive) 중심, 2026-09

`source/m1_mirror` 만 가지고 phonopy 4.4.0 으로 독립 계산
(`symmetrize_force_constants_by_space_group` + level 3, Gonze NAC).

## 0. 결론

**m1 의 기록된 Weyl 위치는 전부 맞다.** 두 계열 모두 재현됐다.

| 계열 | 위치 (native 환산) | gap | χ | 주파수 |
|---|---|---|---|---|
| polar=0 평면 | **(0.0637716, 0, 0.1294181)** | 3.1e-07 THz | **+1** | **10.365955 THz** |
| 〃 (k3 부호 반전 짝) | (0.0637716, 0, −0.1294181) | 3.1e-07 | **−1** | 10.365955 |
| 완전 일반위치 | **(0.4189473, 0.3952085, 0.2625841)** | 3.7e-07 THz | **−1** | **11.671475 THz** |

χ 는 반지름 0.002 / 0.004 / 0.008 1/Å 에서 전부 정수로 안정.
(자체 구현 Berry flux 는 Simphony 와 **전체 부호 규약이 반대**다. 상대 부호와
크기만 비교할 것.)

축 규약: `mirror = k1, k3` / `polar = k2` — spglib 로 재확인, `CLAUDE.md` §2.1 과 일치.

## 1. Born 전치 문제 — 위치에 관한 한 결론 났다 (`CLAUDE.md` §1.3)

phonopy 를 기준값으로 두면 바로 갈린다. 두 후보를 phonopy 로 직접 재면:

| 규약 | 기록된 위치 | **phonopy 에서의 gap** | 판정 |
|---|---|---|---|
| **비전치** | (0.06377, 0, 0.12942) | **4.7e-06 THz** | **노드** |
| 전치 | (0.08115, 0, 0.14476) | **5.99e-02 THz** | 노드 아님 (5자리 차이) |

그리고 `polar=0` 평면을 201×201 (간격 0.0025) 로 훑어 최저점을 격자 미세화하면
**(k1, k3) = (0.0637716, 0.1294181)**, gap 3.9e-07, E 10.365955 THz 로 수렴한다.
`CLAUDE.md` §2.2 의 비전치 기록과 소수점 5자리까지 일치한다.

**즉 Simphony 에 Born 을 넘길 때 비전치가 phonopy 와 맞는 규약이다.**
§1.3 의 원리적 논증(phonopy C 코드가 `np.dot(q, born[i])` 로 첫 인덱스를 축약)과도
같은 방향이다.

남는 것: §1.3 이 전치 쪽 근거로 들었던 "m1 에서 전치로 돌리면 chirality
−1,−1,+1,+1 이 재현된다" 는 여전히 설명되지 않았다. 다만 위치 검사는 정량적이고
5자리 차이로 갈리므로 그쪽이 결정적이다. 또한 이 검사는 **BORN 파일 자체가 올바른
규약으로 저장됐는지**(DFT 쪽 문제)는 말해주지 않는다 — 그건 별개다.

## 2. ⚠️ 탐색 방법의 결함 — gap 순위로 자르면 진짜 노드를 놓친다

**이게 이번 감사의 핵심이다.** 기약 쐐기 N=61 (226,981점) 에서 대칭 강제 자리를
마스킹하고 남은 일반위치 후보를 gap 순으로 세우면, m1 의 **실제 노드 두 개가**

| 노드 | 최근접 격자점 | 그 점의 gap | **순위** |
|---|---|---|---|
| polar=0 (0.06377, 0, 0.12942) | (0.0667, 0.0000, 0.1333) | 1.24e-02 | **907위** |
| 일반위치 (0.41895, 0.39521, 0.26258) | (0.4167, 0.3917, 0.2667) | 1.65e-02 | **1970위** |

1위 후보의 격자 gap 은 1.58e-03 이다. **진짜 노드보다 8~10배 얕아 보인다.**

상위 8개를 전부 미세화하면 결과는 이렇다:

```
(0.4917,0.3750,0.2583) -> (0.500000,0.376812,0.258404)  거울면 k1=0.5
(0.4917,0.1583,0.4917) -> (0.500000,0.147233,0.500000)  거울면
(0.4917,0.1667,0.4917) -> (0.500000,0.155566,0.500000)  거울면
(0.4917,0.1500,0.4917) -> (0.500000,0.138900,0.500000)  거울면
(0.3833,0.4917,0.2750) -> (0.394434,0.500000,0.263900)  polar=0.5 nodal plane
(0.4833,0.1583,0.4917) -> (0.487657,0.156944,0.500000)  거울면
(0.3750,0.4917,0.2750) -> (0.386100,0.500000,0.263900)  polar=0.5 nodal plane
(0.4917,0.1417,0.4917) -> (0.500000,0.130566,0.500000)  거울면
```

**8개 전부 대칭이 강제하는 자리(χ=0)로 배수된다. 진짜 노드는 하나도 없다.**

### 왜 이렇게 되나

콘이 심하게 기울어 있어서(type-II, m1 의 tilt 는 축에 따라 27 / 0.05 / 290) 노드
주변 골이 **얕고 넓다**. 반면 nodal plane 과 거울선은 평면/선 전체가 정확히 축퇴라
그 근처 격자점은 어디를 찍어도 gap 이 작게 나온다. 그래서 **대칭 강제 자리가 격자
gap 순위를 싹 쓸어간다.**

### 그래서 지켜야 할 것

1. **gap 순위로 후보를 자르지 말 것.** 상위 N개만 미세화하는 절차는 이 물질에서
   작동하지 않는다.
2. **미세화 도달점이 대칭면인지로 걸러낼 것.** 판정 기준은 gap 크기가 아니라
   "일반위치에 머무는가" + "χ 가 여러 반지름에서 정수로 안정한가".
3. **어떤 구조에 대해서도 "노드는 이것뿐"이라고 말할 수 없다.** 알려진 노드조차
   907위/1970위다. 완전성 주장은 근거가 없다.

알려진 m1 노드들이 원래 어떻게 발견됐는지 보면 납득이 된다 — polar=0 계열은
Simphony BulkGap 401² 로 **그 평면만** 조밀하게 훑어서 찾았지, 3D 쐐기 순위로
찾은 게 아니다.

## 3. m2.5 도 같은 이유로 틀려 있었다

`results/mirror_strained/M25_RECHECK.md` 참조. 요약하면 `CLAUDE.md` §2.3 과
`results/weyl_trend/SUMMARY.md` 가 "m2.5 에 Weyl 없음" 이라고 적고 있었으나 틀렸다.
실제로는 **(0.2076817, 0.2955478, 0.4345807), χ=+1, 11.878864 THz** 가 있다.
그 노드는 N=61 순위에서 7위였고, 상위 4개만 미세화해서 잘려나갔다.
(두 문서 모두 정정 완료.)

## 4. 대칭 강제 평면 재확인 (무작위 200점씩, m1)

| 평면 | gap 최소 | 중앙값 |
|---|---|---|
| **`k2 = 0.5` (편극축 nodal plane)** | **4.85e-10** | **4.98e-08** |
| `k2 = 0.0` | 6.17e-03 | 3.44e-01 |
| `k1 = 0.0` | 6.54e-06 | 2.05e-01 |
| `k1 = 0.5` | 1.60e-03 | 1.56e-01 |
| `k3 = 0.0` | 7.96e-03 | 3.81e-01 |
| `k3 = 0.5` | 3.46e-05 | 9.94e-02 |

`k2 = 0.5` 만 평면 전체가 축퇴 (2₁ screw + 시간역전). 나머지 거울면은 그 안의
nodal **line** 에서만 축퇴한다. `CLAUDE.md` §2.1·§2.2 와 일치.

## 재현

```bash
python3 scripts/m25_verify.py source/m1_mirror        # 평면 + 쐐기 + 전후보 미세화
python3 scripts/weyl_chirality_flux.py source/m1_mirror 17 0.0637716 0.0  0.1294181
python3 scripts/weyl_chirality_flux.py source/m1_mirror 17 0.0637716 0.0 -0.1294181
python3 scripts/weyl_chirality_flux.py source/m1_mirror 17 0.4189473 0.3952085 0.2625841
```

## 관련 그림 (m1 = m0.8 = m1_mirror, 같은 구조)

라벨 주의: `CLAUDE.md` 는 공칭값으로 **m1 (−1%)**, `figs/` 는 실측값으로
**−0.8%** 라고 부른다. `results/weyl_trend/SUMMARY.md` 의 strain 감사 결과
실제 변형률은 **−0.844% / −0.687%** (두 거울축이 다르다).

| 그림 | 내용 |
|---|---|
| **`figs/fig8_weyl_shift.png`** | **4점 궤도가 unstrained → −0.8% 로 이동. `(±0.14649, ±0.07085)` → `(±0.12942, ±0.06377)`, \|Δk\|=0.0185. 거울선 쪽으로 다가가 소멸한다는 것을 보여주는 그림** |
| `figs/fig7_strain_overlay.png` | 0 / −0.8 / −1.0 / −2.5 / −3.0 % 밴드 중첩 (데이터 `figs/bulkek_m08.dat`) |
| `results/weyl_trend/weyl_trend_k2eq0.png` | polar=0 평면 위 Weyl 위치 추이 (pristine–m1–p1 선 연결) |
| `figs/arc_m08.png`, `arc_m08_gaponly.png`, `arc_m08_zoom.png` | m1 표면 아크 맵 (결론: 현재 슬랩에서 아크 증거 없음 — `results/arc_check/SUMMARY.md` §7·§8) |

`figs/` 는 표준 (a,b,c) 규약(편극축=c)이고 `CLAUDE.md` 는 native 규약이라
좌표 순서가 다르다. m1 은 `native (k1,k2,k3) = 표준 (k_b, k_c, k_a)` 이므로
native `(0.06377, 0, 0.12942)` = 표준 `(0.12942, 0.06377, 0)` — 같은 점이다.
